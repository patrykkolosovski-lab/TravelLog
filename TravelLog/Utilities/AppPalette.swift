import SwiftUI

enum AppPalette {
    static let lime = Color(red: 0.62, green: 0.88, blue: 0.08)
    static let cityRed = Color(red: 0.86, green: 0.16, blue: 0.18)
    static let unvisited = Color(red: 0.57, green: 0.59, blue: 0.60)
    static let mapBackground = Color.black
    static let mapBoundary = Color.white.opacity(0.72)
    static let windowBackground = Color(nsColor: .windowBackgroundColor)
}
